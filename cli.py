"""
VeriFact CLI Entry Point

This module provides a command-line interface for the VeriFact application.
It's intended for quick testing and basic interactions without using the web UI.
For the web interface, use `app.py` with Chainlit, and for API access, use `src/main.py`.
"""

def main():
    """Run the VeriFact CLI."""
    print("VeriFact CLI - For a better experience, use the Chainlit UI with 'chainlit run app.py'")
    # TODO: Implement actual CLI functionality
    # Example: Ask for input text, run factcheck pipeline, display results


if __name__ == "__main__":
    main()
